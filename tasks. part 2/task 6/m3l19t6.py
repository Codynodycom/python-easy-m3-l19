'''
Задача 6

Вы решили усовершенствовать программу для шифрования сообщений с использованием шифра 
Виженера, чтобы она также поддерживала дешифровку зашифрованных сообщений. Для этого 
вам нужно дополнить функцию так, чтобы она могла не только зашифровывать, но и расшифровывать сообщения.

Шифр Виженера использует ключевое слово, чтобы определить сдвиг для каждой буквы 
в сообщении. Для дешифровки необходимо применить обратный сдвиг.

Вам нужно дописать функцию vigenere_cipher(message, keyword, decrypt=False),
которая будет принимать сообщение, ключевое слово и флаг decrypt (по умолчанию равен False), а 
затем возвращать зашифрованное или расшифрованное сообщение в зависимости от значения флага.
'''

def vigenere_cipher(message, keyword, decrypt=False):
    result = ""
    keyword_length = len(keyword)
    alphabet = "abcdefghijklmnopqrstuvwxyz"

    # Преобразуем ключевое слово в числовой ключ

    # Проходим по каждому символу в сообщении

        # Находим сдвиг для текущего символа
    
        # Применяем обратный сдвиг, если decrypt=True
    
        # Шифруем/расшифровываем символ с помощью сдвига
    
            # Если символ не буква, оставляем его без изменений


# Пример использования для зашифровки
message = "hello, world!"
keyword = "python"
encrypted_message = vigenere_cipher(message, keyword)
print("Зашифрованное сообщение:", encrypted_message)

# Пример использования для расшифровки
decrypted_message = vigenere_cipher(encrypted_message, keyword, decrypt=True)
print("Расшифрованное сообщение:", decrypted_message)

